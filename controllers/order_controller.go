package controllers

/*import (
	"net/http"
	"sanbercode-final-project-Sahrul-Yoyo/models"

	"github.com/gin-gonic/gin"
	// Sesuaikan dengan path package models Anda
	//"github.com/yourusername/yourproject/models"
)
*/
// GetOrders mengambil daftar pesanan dari database dan mengembalikannya sebagai response JSON
/*func GetOrders(c *gin.Context) {
	// Mendapatkan daftar pesanan dari database
	orders, err := models.GetAllOrders() // Menggunakan fungsi GetAllOrders dari package models
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{
			"status":  "error",
			"message": "Gagal mengambil daftar pesanan",
		})
		return
	}

	// Mengirim daftar pesanan sebagai response JSON
	c.JSON(http.StatusOK, gin.H{
		"status":  "success",
		"message": "Daftar pesanan berhasil diambil",
		"orders":  orders,
	})
}*/
