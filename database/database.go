package database

/*import (
	"fmt"
	"sanbercode-final-project-Sahrul-Yoyo/config"

	"github.com/jinzhu/gorm"
	_ "github.com/lib/pq" // PostgreSQL driver
)

// ConnectToDB establishes connection to the database
func ConnectToDB(cfg *config.Config) (*gorm.DB, error) {
	dsn := fmt.Sprintf("host=%s user=%s password=%s dbname=%s sslmode=disable", cfg.DBHost, cfg.DBUser, cfg.DBPassword, cfg.DBName)
	db, err := gorm.Open("postgres", dsn)
	if err != nil {
		return nil, err
	}

	return db, nil
}
*/
