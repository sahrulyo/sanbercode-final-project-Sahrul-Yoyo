package service

/*import (
	"errors"
	"sanbercode-final-project-Sahrul-Yoyo/config"
	"sanbercode-final-project-Sahrul-Yoyo/models"
)

// ValidateUser validates user credentials and returns a JWT token
func ValidateUser(username, password string, cfg *config.Config) (string, error) {
	// ... implement user authentication logic
	if username != "admin" || password != "password123" {
		return "", errors.New("invalid credentials")
	}

	// ... generate and return JWT token
	token, err := GenerateJWT(&models.User{
		Username: username,
		Email:    "admin@example.com",
	}, cfg)
	if err != nil {
		return "", err
	}

	return token, nil
}
*/
